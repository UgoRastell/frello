{% extends 'base.html.twig' %}

{% block title %}Vôtre tableau{% endblock %}

{% block body %}

    <nav class="navbar navbar-expand-lg bg-body-tertiary ">
        <div class="container-fluid">
            <h1>{{ board.libelle }}</h1>
        <a class="btn btn-warning" style="margin-left:1000px;" href="{{ path('app_board_edit', {'id': board.id}) }}">Modifier</a>
        {{ include('board/_delete_form.html.twig') }}
        </div>
    </nav>

    <table class="table">
        <tbody>
           
        </tbody>
    </table>
    
{% endblock %}
